    var color = ["red", "blue", "yellow", "pink","green"];
    for(var i=0;i=color.length;i++){
    	    var button=$("<button>");
    		button.text(color[i]);
    		$("#buttons").append(button);
    };
